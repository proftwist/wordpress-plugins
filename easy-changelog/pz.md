# Пояснительная записка к плагину Easy Changelog

## Описание проекта

Плагин Easy Changelog представляет собой WordPress блок для отображения истории изменений (changelog) с поддержкой внешних JSON из GitHub и других источников. **Версия 2.0+ включает полную автоматизацию с GitHub webhooks для мгновенного обновления данных.**

## Полная автоматизация с GitHub Webhooks (версия 2.0.0+)

### 🚀 Ключевая особенность: 100% автоматизация workflow

**Основной принцип:**
```
Разрабатываете → Коммитите → Пушите → Данные обновляются АВТОМАТИЧЕСКИ
```

### 🔧 Техническая реализация webhooks

**1. Система отслеживания блоков через БД**
- Таблица `easy_changelog_blocks` для хранения отслеживаемых блоков
- Автоматическое сохранение `post_id`, `block_id`, `external_url` при создании/обновлении блока
- Ежедневная очистка устаревших записей (7+ дней)

**2. Webhook endpoint**
- URL: `/easy-changelog/v1/github-webhook`
- Обработка GitHub push events
- Автоматическое обновление только затронутых файлов при push
- Фоновая обработка без влияния на производительность

**3. Интерфейс редактора с автосинхронизацией**
- Кнопка "Копировать webhook URL" в редакторе
- Визуальные индикаторы статуса автосинхронизации:
  - 🟢 **Зеленый**: Автосинхронизация активна
  - 🟡 **Желтый**: Настройка webhook требуется
  - 🔴 **Красный**: Ошибка синхронизации

### ⚡ Преимущества автоматизации

- **Мгновенное обновление**: Данные обновляются за секунды после push
- **Автоматическое отслеживание**: Не нужно вручную обновлять данные
- **Производительность**: Обновляются только затронутые файлы
- **Безопасность**: Валидация webhook payload от GitHub

### 🛠️ Настройка для пользователей

**Инструкция:**
1. Добавьте блок Easy Changelog в пост/страницу
2. В редакторе нажмите "Копировать webhook URL"
3. В GitHub репозитории добавьте webhook:
   - Payload URL: `ваш_сайт/wp-json/easy-changelog/v1/github-webhook`
   - Content type: `application/json`
   - Events: `Just the push event`
4. Сохраните webhook - автоматизация активирована!

## Исправления версии 2.0.3 (25.11.2025) - Безопасность и производительность

### 🔒 Исправления безопасности WordPress

**Проблемы безопасности:**
- Строка 643: Ошибка экранирования вывода `__()` - output not escaped
- Строка 109: Deprecated функция `load_plugin_textdomain()` (WordPress 4.6+)
- Строки 419, 600, 607: Неподготовленные SQL запросы с интерполяцией переменных
- Строки 265, 283, 465, 633, 640: Прямые запросы к БД без обоснования

**Решения:**
- ✅ Заменен `__()` на `esc_html__()` для безопасного экранирования вывода
- ✅ Убрана ручная загрузка текстового домена (WordPress делает это автоматически)
- ✅ Все SQL запросы переведены на использование `$wpdb->prepare()` с placeholders
- ✅ Добавлены пояснительные комментарии для прямых обращений к `$wpdb`
- ✅ Создан отдельный метод `query_blocks_by_url()` для операций чтения

### ⚡ Исправления производительности БД

**Проблемы:**
- Прямые запросы к БД без кеширования
- Отсутствие оптимизации для повторяющихся запросов

**Решения:**
- ✅ Добавлено кеширование через `wp_cache_get/wp_cache_set` для запросов поиска блоков
- ✅ Кеширование результатов на 5 минут для улучшения производительности
- ✅ Автоматическая очистка кеша при обновлении данных блока
- ✅ Оптимизация запросов в функции `update_affected_blocks()`
- ✅ Разделение операций чтения (с кешированием) и записи (без кеширования)

### 📝 Соответствие WordPress Plugin Check

**Добавленные комментарии:**
- Для операций записи: "Прямое обращение к $wpdb оправдано для операций записи"
- Для операций обновления: "Операция обновления в БД, кеширование не требуется"
- Для операций очистки: "Прямое обращение к $wpdb оправдано для операций очистки"
- Для операций чтения: Вынесено в отдельный метод с кешированием

### 🛠️ Технические детали исправлений

**Безопасность SQL:**
```php
// БЫЛО (небезопасно):
"SELECT * FROM $table_name WHERE json_url LIKE %s"

// СТАЛО (безопасно):
"SELECT * FROM {$table_name} WHERE json_url LIKE %s"
$wpdb->prepare("SELECT * FROM {$table_name} WHERE json_url LIKE %s", $file_url . '%')
```

**Безопасность вывода:**
```php
// БЫЛО:
echo __('История изменений', 'easy-changelog');

// СТАЛО:
echo esc_html__('История изменений', 'easy-changelog');
```

**Кеширование БД:**
```php
// Добавлено кеширование запросов:
$cache_key = 'easy_changelog_blocks_' . md5($file_url . '%');
$blocks = wp_cache_get($cache_key, 'easy_changelog');

if (false === $blocks) {
    $blocks = $wpdb->get_results($wpdb->prepare(...));
    wp_cache_set($cache_key, $blocks, 'easy_changelog', 5 * MINUTE_IN_SECONDS);
}
```

## Критические исправления версии 2.0.1

### 🔧 Исправлена проблема создания таблиц БД

**Проблема:**
- Таблица `easy_changelog_blocks` не создавалась при активации плагина
- `register_activation_hook` вызывался внутри класса - неправильно
- Система отслеживания блоков не работала

**Решение:**
- Перенесены функции активации/деактивации ВНЕ класса
- Добавлена система проверки версии БД через `check_db_version()`
- Улучшенная логика создания таблиц с логированием процесса
- Добавлены отладочные логи для отслеживания блоков при сохранении постов

## Исправление стилей фронтенда (версия 2.0.0)

### 🎨 Добавлены отсутствующие CSS стили

**Проблема:**
- На фронтенде не отображались стили блока
- Блок выглядел неформатированным

**Решение:**
- Создан файл `build/style-index.css` с полным набором стилей
- Основные стили блока changelog с адаптивным дизайном
- Зеленые маркеры для added элементов, синие для fixed
- Поддержка темной темы, высокой контрастности, мобильной адаптивности
- Print стили и accessibility поддержка

## Предыдущие улучшения версии 1.3.0

### 🔧 Исправление проблемы кеширования GitHub

**Проблема:**
- GitHub raw файлы кешируются на 5-10 минут
- После обновления файла плагин показывал устаревшие данные

**Решение:**
- Функция `refreshGitHubUrl()` в JavaScript с timestamp параметром
- Кеширование GitHub URLs на 2 минуты (вместо 1 часа)
- Игнорирование timestamp параметров при кешировании

## Техническая архитектура

### Система компонентов

#### 1. PHP ядро (easy-changelog.php v2.0.1)
- **Webhook функциональность**: Система отслеживания блоков через БД
- **REST API endpoints**:
  - `/easy-changelog/v1/github-webhook` - прием push events
  - `/easy-changelog/v1/fetch-external` - загрузка внешних данных
- **База данных**: Создание и управление таблицей `easy_changelog_blocks`
- **Система очистки**: Ежедневная очистка устаревших записей
- **Логирование**: Отладочные логи для диагностики

#### 2. React редактор (src/edit.js v2.0.0)
- **Автосинхронизация**: Автоматическое обновление при GitHub push
- **Интерфейс webhook**: Копирование URL, визуальные индикаторы
- **Трехвкладочный интерфейс**: JSON, Внешний JSON, Предпросмотр
- **Валидация**: JSON в реальном времени с понятными ошибками

#### 3. Система сборки (package.json v2.0.1)
- **WordPress Scripts**: Официальная система сборки
- **Зависимости**: React 18+, WordPress components
- **Стили**: SCSS с автопрефиксером
- **Производственная сборка**: Оптимизированные assets

### База данных

#### Таблица `easy_changelog_blocks`
```sql
CREATE TABLE easy_changelog_blocks (
    id bigint(20) NOT NULL AUTO_INCREMENT,
    post_id bigint(20) NOT NULL,
    block_id varchar(255) NOT NULL,
    external_url varchar(500) NOT NULL,
    created_at datetime DEFAULT CURRENT_TIMESTAMP,
    updated_at datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    UNIQUE KEY unique_block (post_id, block_id),
    KEY idx_post_id (post_id),
    KEY idx_external_url (external_url)
);
```

## Ключевые возможности

### Функциональность блока
- **Автоматическая синхронизация**: Мгновенное обновление при GitHub push
- **Внешние JSON**: Поддержка GitHub и других источников
- **Предпросмотр**: Реальное время при редактировании
- **Типы изменений**: added (зеленые), fixed (синие) маркеры
- **Адаптивность**: Корректное отображение на всех устройствах

### Безопасность
- **Валидация webhook**: Проверка payload от GitHub
- **Экранирование данных**: Все пользовательские данные
- **Права доступа**: Только авторизованные пользователи
- **URL валидация**: Предотвращение SSRF атак

### Производительность
- **Фоновая обработка**: Webhook без блокировки
- **Умное обновление**: Только затронутые файлы
- **Кеширование**: Оптимальное время для разных источников
- **Ленивая загрузка**: Assets только при необходимости

## Технологический стек

### Frontend
- **React 18** с hooks
- **WordPress Components** (@wordpress/components)
- **WordPress Block API** для регистрации блоков
- **SCSS** с автопрефиксером и PostCSS

### Backend
- **PHP 7.4+** с современным синтаксисом
- **WordPress REST API** для endpoints
- **MySQL/MariaDB** для хранения отслеживаемых блоков
- **WordPress Hooks** для интеграции

### Сборка и разработка
- **WordPress Scripts** (@wordpress/scripts)
- **Node.js 14+** для разработки
- **Webpack** для bundling
- **Babel** для транспиляции

## Структура данных

### JSON формат для changelog
```json
[
  {
    "version": "2.0.0",
    "date": "20.11.2025",
    "added": [
      "Полная автоматизация с GitHub webhooks",
      "Мгновенное обновление данных при push"
    ],
    "fixed": [
      "Исправлена проблема создания таблиц БД",
      "Добавлены отсутствующие CSS стили"
    ]
  }
]
```

### Атрибуты блока
- **jsonUrl**: URL внешнего JSON файла
- **useExternalUrl**: Использовать внешний JSON
- **blockId**: Уникальный идентификатор блока (автоматически)

## Локализация

### Поддерживаемые языки
- **Русский** (ru_RU) - по умолчанию
- **Английский** (en_US)

### Новые строки в v2.0+
- Автосинхронизация
- Настройка webhook
- Статус соединения
- Копировать webhook URL
- GitHub webhook настроен
- Требуется настройка webhook

## Процесс развертывания

### Разработка
```bash
cd easy-changelog
npm install          # Установка зависимостей
npm start            # Разработка с hot reload
npm run build        # Production сборка
```

### Production установка
1. Загрузить папку плагина в `/wp-content/plugins/`
2. Активировать через админ-панель
3. Настроить webhook в GitHub (см. инструкцию выше)

## История версий

### 2.0.3 (25.11.2025) - Безопасность и производительность
- **Исправлены все проблемы безопасности WordPress Plugin Check**
- Заменен небезопасный вывод `__()` на `esc_html__()`
- Убрана deprecated функция `load_plugin_textdomain()`
- Все SQL запросы переведены на подготовленные statements
- Добавлено кеширование запросов БД для улучшения производительности
- Оптимизированы прямые запросы к базе данных с `wp_cache_get/set`

### 2.0.2 (20.11.2025) - Тестовая запись
- Тестовая запись для проверки добавления новых релизов
- Улучшения производительности системы отслеживания блоков

### 2.0.1 (20.11.2025) - Критическое исправление
- **Исправлена критическая проблема с созданием таблиц БД**
- Перенесены функции активации/деактивации ВНЕ класса
- Добавлена система проверки версии БД
- Улучшенная логика создания таблиц с логированием

### 2.0.0 (20.11.2025) - Полная автоматизация
- **Полная автоматизация с GitHub webhooks**
- Система отслеживания блоков через БД
- Мгновенное обновление данных при push events
- Исправлены отсутствующие стили фронтенда
- Интерфейс для копирования webhook URL
- Визуальные индикаторы статуса автосинхронизации

### 1.3.0 (20.11.2025) - Поддержка внешних JSON
- Исправлена проблема кеширования GitHub raw ссылок
- Поддержка внешних JSON из GitHub и других источников
- Трехвкладочный интерфейс редактора
- REST API endpoint для загрузки внешних данных
- Принудительное обновление GitHub URLs с timestamp

### 1.2.0 (20.11.2025) - Поддержка fixed
- Исправление стилей для редактора Гутенберга
- Поддержка тега fixed для исправлений (синие маркеры)
- Улучшенная визуализация предпросмотра
- Поддержка темной темы и accessibility

### 1.1.0 (19.11.2025) - Первоначальный релиз
- Базовый Gutenberg блок для changelog
- Редактор JSON с предпросмотром
- Локализация на русский и английский

## Совместимость

### Системные требования
- **WordPress**: 5.8+
- **PHP**: 7.4+
- **Node.js**: 14+ (для разработки)
- **MySQL**: 5.6+

### Поддерживаемые браузеры
- Chrome 90+, Firefox 88+, Safari 14+, Edge 90+

## API для разработчиков

### CSS классы для кастомизации
```css
.easy-changelog-block     /* Основной контейнер */
.easy-changelog-release   /* Контейнер релиза */
.easy-changelog-version   /* Версия */
.easy-changelog-date      /* Дата */
.easy-changelog-added     /* Список added (зеленые маркеры) */
.easy-changelog-fixed     /* Список fixed (синие маркеры) */
```

### WordPress хуки
```php
// Фильтр для изменения данных перед рендерингом
add_filter('easy_changelog_data', function($data, $attributes) {
    // Кастомная обработка данных
    return $data;
}, 10, 2);

// Действие после обновления через webhook
add_action('easy_changelog_updated', function($post_id, $block_id) {
    // Логика после обновления блока
}, 10, 2);
```

## Поддержка

### Техническая информация
- **Автор**: Владимир Бычко (http://bychko.ru)
- **Лицензия**: GPL v2 или более поздняя
- **Текущая версия**: 2.0.3
- **Дата последнего обновления**: 25.11.2025

### Документация
- Подробные инструкции в README.md
- Примеры webhook настройки в редакторе блоков
- API документация для разработчиков
- Руководство по troubleshooting

---

**Плагин разработан с соблюдением стандартов WordPress Codex, лучших практик безопасности и современных принципов автоматизации разработки.**