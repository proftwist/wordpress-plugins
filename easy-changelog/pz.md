# Пояснительная записка к плагину Easy Changelog

## Описание проекта

Плагин Easy Changelog представляет собой WordPress блок для отображения истории изменений (changelog) с поддержкой внешних JSON из GitHub и других источников.

## Исправления проблемы кеширования GitHub (версия 1.3.0)

### 🔧 Исправлена критическая проблема с кешированием GitHub raw ссылок

**Проблема:**
GitHub имеет два разных CDN:
- Веб-интерфейс (`github.com`) - обновляется мгновенно ✅
- Raw файлы (`raw.githubusercontent.com`) - кешируются на 5-10 минут ❌

Это приводило к тому, что после обновления файла в репозитории, плагин показывал устаревшие данные в течение 5-10 минут.

### ✅ Решение

**1. Принудительное обновление URL в JavaScript**
- Добавлена функция `refreshGitHubUrl()` в `src/edit.js`
- Автоматически добавляет timestamp к GitHub URLs при загрузке
- Формат: `?t={timestamp}` для принудительного обновления кеша

**2. Улучшенная логика кеширования в PHP**
- Для GitHub URLs: кеширование на **2 минуты** (вместо 1 часа)
- Для других источников: кеширование на **1 час**
- Игнорирование параметров при кешировании (удалиние `?t=timestamp` из ключа)

**3. Оптимизированная логика кеширования**
```php
// Для GitHub кешируем на 2 минуты, для остальных - на 1 час
$cache_time = HOUR_IN_SECONDS;
if (strpos($url, 'raw.githubusercontent.com') !== false) {
    $cache_time = 2 * MINUTE_IN_SECONDS;
}
```

### 🎯 Результат
- **При загрузке через плагин** - всегда актуальные данные благодаря timestamp
- **На фронтенде сайта** - разумное кеширование на 2 минуты для GitHub
- **Производительность** - значительно улучшена за счет оптимального времени кеширования

## Обновления версии 1.3.0

### 🆕 Ключевая новая функциональность

**Поддержка внешних JSON файлов из GitHub и других источников**
- Трехвкладочный интерфейс редактора: "Редактор JSON", "Внешний JSON", "Предпросмотр"
- Автоматическая загрузка changelog данных из внешних URL
- Кеширование внешних запросов на 1 час для повышения производительности
- Graceful fallback на локальные данные при ошибках загрузки

### 🔧 Технические улучшения

**REST API endpoint для безопасной загрузки**
- Создан собственный endpoint `/easy-changelog/v1/fetch-external`
- Валидация URL и JSON данных для безопасности
- Проверка прав доступа (только пользователи с правами `edit_posts`)

**Улучшенный интерфейс редактора**
- Автоматическое преобразование GitHub URL в raw URL
- Подробные инструкции по получению ссылок на GitHub
- Визуальные индикаторы статуса загрузки (успех/ошибка/загрузка)
- Поддержка новых атрибутов блока: `jsonUrl` и `useExternalUrl`

**Расширенная локализация**
- Добавлены переводы для всех новых текстов интерфейса
- Полная поддержка русского и английского языков

## Обновления версии 1.2.1

### Критические исправления
- **Исправление отображения fixed элементов в предпросмотре** - Теперь fixed элементы корректно отображаются в редакторе Гутенберга
- **Улучшена функция renderList** - Добавлена проверка на пустые массивы для предотвращения создания пустых HTML элементов
- **Обновлен текст подсказки** - Добавлено упоминание поддержки fixed в помощи редактора
- **Упрощена логика рендеринга** - Улучшена производительность и читаемость кода

## Обновления версии 1.2.0

### Ключевые улучшения
- **Исправление стилей для редактора Гутенберга** - Улучшена совместимость и отображение в редакторе блоков
- **Поддержка тега `fixed`** - Добавлена возможность отображения исправлений с синими маркерами
- **Улучшенная визуализация предпросмотра** - Предпросмотр теперь точно соответствует фронтенду
- **Разделение типов изменений** - Зеленые маркеры для `added`, синие для `fixed`
- **Поддержка темной темы** - Специальные стили для темного режима редактора
- **Улучшенная адаптивность** - Оптимизация для мобильных устройств
- **Анимации и переходы** - Плавные анимации для улучшения пользовательского опыта
- **Поддержка accessibility** - Высокая контрастность и уменьшение движения

## Техническая реализация

### Архитектура
- **Базовый язык**: PHP 7.4+
- **Frontend**: React 18+ с использованием @wordpress/components
- **Сборка**: WordPress Scripts (@wordpress/scripts)
- **Стили**: SCSS с автопрефиксером и PostCSS
- **Локализация**: WordPress i18n с поддержкой русского и английского языков

### Новые компоненты системы

#### 1. Расширенный PHP класс (easy-changelog.php)
- Регистрация Gutenberg блока с новыми атрибутами
- Рендеринг блока на фронтенде с поддержкой внешних JSON
- Подключение локализации с новыми переводами
- Добавление категории блоков
- **Новое**: REST API endpoint `/easy-changelog/v1/fetch-external`
- **Новое**: Метод `fetch_external_json()` с кешированием
- **Новое**: Метод `handle_external_fetch()` для обработки запросов

#### 2. React компоненты (src/)
- **index.js**: Регистрация блока с новыми атрибутами `jsonUrl` и `useExternalUrl`
- **edit.js**: Трехвкладочный интерфейс с поддержкой внешних JSON
- **style.scss**: Переработанные стили для редактора и фронтенда

#### 3. Система локализации (languages/)
- **easy-changelog-ru_RU.po**: Расширенная русская локализация
- **easy-changelog-en_US.po**: Расширенная английская локализация

#### 4. Система сборки (package.json)
- **Новое**: Добавлен package.json с зависимостями
- **Новое**: Поддержка WordPress Scripts для сборки
- **Новое**: Скрипты для разработки и production

## Ключевые особенности

### Функциональность редактора (обновлено в 1.3.0)
- **Трехвкладочный интерфейс**: "Редактор JSON", "Внешний JSON", "Предпросмотр"
- Загрузка данных из внешних URL с визуальной обратной связью
- Автоматическое преобразование GitHub ссылок в raw формат
- Валидация JSON в реальном времени
- Автоматическое обновление предпросмотра
- Обработка ошибок с понятными сообщениями
- **Поддержка тега `fixed` для отображения исправлений**

### Безопасность (улучшено в 1.3.0)
- Экранирование всех выводимых данных (esc_html)
- Валидация JSON данных перед рендерингом
- Проверка наличия Gutenberg перед регистрацией блока
- **Новое**: REST API с проверкой прав доступа
- **Новое**: Валидация URL для предотвращения атак

### Производительность (улучшено в 1.3.0)
- Ленивая загрузка фронтенд стилей
- Минимальное количество зависимостей
- Оптимизированная сборка через WordPress Scripts
- **Новое**: Кеширование внешних запросов с оптимальным временем

## Технологический стек

### Зависимости времени выполнения
```json
{
  "@wordpress/blocks": "^12.0.0",
  "@wordpress/block-editor": "^11.0.0",
  "@wordpress/components": "^23.0.0",
  "@wordpress/element": "^5.0.0",
  "@wordpress/i18n": "^4.0.0",
  "@wordpress/api-fetch": "^6.0.0"
}
```

### Зависимости разработки
```json
{
  "@wordpress/scripts": "^25.0.0"
}
```

## Структура данных

### Формат JSON для changelog (без изменений)
```json
[
  {
    "version": "1.0.0",
    "date": "19.11.2025",
    "added": ["Первоначальный релиз плагина", "Базовая функциональность блоков"],
    "fixed": ["Исправлена ошибка валидации JSON", "Улучшена обработка дат"]
  }
]
```

### Новые атрибуты блока (1.3.0)
- **jsonUrl**: URL внешнего JSON файла (строка)
- **useExternalUrl**: Использовать внешний JSON или нет (boolean)

### Поддерживаемые поля
- **version**: Версия релиза (строка)
- **date**: Дата релиза (строка)
- **added**: Массив добавленных функций (array of strings) - зеленые маркеры
- **fixed**: Массив исправлений (array of strings) - синие маркеры

## Обновленные стили

### Новые CSS классы (без изменений)
- `.easy-changelog-added` - список добавленных функций (зеленые маркеры)
- `.easy-changelog-fixed` - список исправлений (синие маркеры)
- `.easy-changelog-item` - отдельный элемент списка
- `.easy-changelog-item-added` - элемент добавленной функции
- `.easy-changelog-item-fixed` - элемент исправления
- `.easy-changelog-preview` - предпросмотр в редакторе

### Специальные возможности стилей (без изменений)
- Поддержка темной темы через `.wp-block-dark`
- Высокая контрастность через `prefers-contrast: high`
- Уменьшение движения через `prefers-reduced-motion: reduce`
- Адаптивность для мобильных устройств
- Плавные анимации и переходы
- Специальные стили для редактора Гутенберга

## Процесс сборки

### Команды разработки (обновлено в 1.3.0)
```bash
# Установка зависимостей
cd easy-changelog
npm install

# Разработка с горячей перезагрузкой
npm start

# Сборка для production
npm run build

# Создание ZIP архива для распространения
npm run plugin-zip

# Компиляция локализации
msgfmt languages/easy-changelog-ru_RU.po -o languages/easy-changelog-ru_RU.mo
msgfmt languages/easy-changelog-en_US.po -o languages/easy-changelog-en_US.mo
```

## Совместимость

### Системные требования
- WordPress 5.8+
- PHP 7.4+
- Node.js 14+ (для разработки)

### Поддерживаемые браузеры
- Chrome 90+
- Firefox 88+
- Safari 14+
- Edge 90+

## Локализация

### Поддерживаемые языки
- **Русский** (ru_RU) - по умолчанию
- **Английский** (en_US)

### Новые строки локализации (1.3.0)
- Внешний JSON
- URL внешнего JSON файла
- Как получить ссылку на GitHub
- Инструкции по получению ссылки GitHub
- Пример GitHub URL
- Ошибка загрузки внешнего JSON
- Данные успешно загружены
- Загрузить из URL
- Использовать внешний JSON
- Введите URL для загрузки
- Не удалось загрузить данные из указанного URL

### Структура переводов
Все текстовые строки обернуты в функции `__()` и поддерживают:
- Контекст перевода ('easy-changelog')
- Множественные формы для русского языка
- Fallback на английский при отсутствии перевода

## Безопасность

### Меры защиты (улучшено в 1.3.0)
- Все пользовательские данные экранируются
- JSON валидируется перед обработкой
- Проверка прав доступа (проверяется в WordPress)
- Защита от XSS через esc_html()
- **Новое**: Валидация URL для предотвращения SSRF атак
- **Новое**: Проверка прав доступа в REST API

### Валидация данных (улучшено в 1.3.0)
- Проверка JSON синтаксиса
- Убеждение что данные являются массивом
- Проверка наличия обязательных полей
- Graceful fallbacks при ошибках
- **Новое**: Валидация URL формата
- **Новое**: Проверка HTTP статуса ответов

## Развертывание

### Установка в production
1. Скопировать папку плагина в `/wp-content/plugins/`
2. Активировать через админ-панель WordPress
3. При необходимости скопировать собранные файлы из build/

### Рекомендуемая структура production (обновлено в 1.3.0)
```
wp-content/plugins/easy-changelog/
├── easy-changelog.php
├── package.json (не обязательно)
├── build/
│   ├── index.js
│   ├── index.asset.php
│   └── style-index.css
├── src/ (не обязательно в production)
└── languages/
    ├── easy-changelog-ru_RU.mo
    ├── easy-changelog-ru_RU.json
    ├── easy-changelog-en_US.mo
    └── easy-changelog-en_US.json
```

## Возможности расширения

### Реализованные улучшения
- ✅ **Новое в 1.3.0**: Исправлена проблема кеширования GitHub raw ссылок
- ✅ **Новое в 1.3.0**: Принудительное обновление URL с timestamp
- ✅ **Новое в 1.3.0**: Оптимизированная логика кеширования (2 мин для GitHub, 1 час для остальных)
- ✅ **Новое в 1.3.0**: Поддержка внешних JSON из GitHub и других источников
- ✅ **Новое в 1.3.0**: REST API для безопасной загрузки данных
- ✅ **Новое в 1.3.0**: Трехвкладочный интерфейс редактора
- ✅ **Новое в 1.3.0**: Автоматическое преобразование GitHub ссылок
- ✅ Поддержка тега `fixed` для отображения исправлений
- ✅ Улучшенные стили для редактора Гутенберга
- ✅ Разделение типов изменений по цветам
- ✅ Поддержка темной темы

### Потенциальные улучшения
- Поддержка других типов изменений (deprecated, removed, security)
- Импорт из GitLab, Bitbucket и других платформ
- Шаблоны для различных форматов changelog
- Экспорт в различные форматы
- Интеграция с системами CI/CD
- **Планируется**: Поддержка webhooks для автоматического обновления

### API для разработчиков
Блок предоставляет чистый HTML output и может быть кастомизирован через CSS. Структура классов:
- `.easy-changelog-block` - основной контейнер
- `.easy-changelog-release` - контейнер релиза
- `.easy-changelog-version` - версия
- `.easy-changelog-date` - дата
- `.easy-changelog-content` - контент релиза
- `.easy-changelog-added` - список добавленных функций (зеленые маркеры)
- `.easy-changelog-fixed` - список исправлений (синие маркеры)

## Поддержка и документация

### Документация для пользователей
- Подробные инструкции по использованию в README.md
- Примеры JSON структур с поддержкой `fixed`
- Советы по форматированию changelog
- **Новое**: Инструкции по получению GitHub ссылок
- **Новое**: Примеры использования внешних JSON

### Техническая поддержка
- Автор: Владимир Бычко (http://bychko.ru)
- Лицензия: GPL v2 или более поздняя
- Версия: 1.3.0

### История версий
- **1.3.0** - Исправлена проблема кеширования GitHub, поддержка внешних JSON, REST API, трехвкладочный интерфейс
- **1.2.1** - Исправление отображения fixed элементов в предпросмотре
- **1.2.0** - Исправление стилей редактора, добавлена поддержка `fixed`
- **1.1.0** - Первоначальный релиз с базовой функциональностью

---

*Плагин разработан с соблюдением стандартов WordPress Codex и лучших практик безопасности.*