# 📘 Пояснительная записка к плагину **Ограничитель размера файлов**

**Тип документа:** Техническая пояснительная записка
**Проект:** WordPress-плагин для ограничения размера загружаемых файлов
**Версия:** 1.1.0
**Разработчик:** Владимир Бычко
**Платформа:** WordPress 5.x+, PHP 7.4+
**Дата:** 25.11.2025

---

## 🎯 Назначение и цель проекта

Плагин **Ограничитель размера файлов** добавляет мощный инструмент для контроля размера загружаемых файлов в WordPress. Он интегрируется в стандартную страницу настроек медиафайлов и позволяет администраторам устанавливать максимальный размер файлов в мегабайтах с наивысшим приоритетом.

**Версия 1.1.0** - Улучшенная версия с переходом на более удобные единицы измерения (мегабайты вместо байтов).

Основные цели разработки:

* Предоставить удобный контроль над размером загружаемых файлов в мегабайтах
* Переопределить системные лимиты WordPress при необходимости
* Обеспечить простой и понятный интерфейс для администраторов
* Поддержать полную локализацию на русский и английский языки
* Создать максимально лёгкое и эффективное решение

---

## 🧩 Ключевые возможности

### Основные функции

* 🎛️ Интеграция в страницу Настройки → Медиафайлы
* 📏 Точное ограничение размера файлов в мегабайтах (МБ)
* 🔝 Наивысший приоритет над другими системными лимитами
* 🌐 Полная поддержка локализации (русский и английский)
* 🛡️ Безопасная валидация и санитизация входных данных
* 📊 Отображение текущих системных лимитов WordPress
* 🚫 Возможность отключения ограничения (значение 0)
* 💡 Подробные подсказки и рекомендации для пользователей

### Интерфейс настроек

* Поле ввода для установки лимита в мегабайтах (с шагом 0.1)
* Валидация отрицательных значений и слишком больших значений (макс. 1024 МБ)
* Информация о текущих системных лимитах в МБ и формате
* Понятные описания функций с указанием единиц измерения
* Автоматическая конвертация существующих настроек из байт в МБ

---

## ⚙️ Архитектура и структура проекта

### 📁 Файловая структура

```
mediafile-limiter/
├── file-size-limit.php    # Основной файл плагина
├── pz.md                        # Пояснительная записка
├── readme.txt                   # Описание плагина
├── release-history.json         # История релизов
└── languages/
    ├── file-size-limit.pot         # Шаблон переводов
    ├── file-size-limit-ru_RU.po    # Русская локализация
    ├── file-size-limit-en_US.po    # Английская локализация
    ├── file-size-limit-ru_RU.mo    # Скомпилированный русский
    └── file-size-limit-en_US.mo    # Скомпилированный английский
```

---

## 🧠 Внутренние компоненты

### 🟦 PHP: `file-size-limit.php`

* Главный класс **Dipsic_File_Size_Limit**
* Методы для работы с настройками:
  * `admin_init()` — регистрация настроек в админке
  * `section_callback()` — описание секции настроек (обновлено для МБ)
  * `field_callback()` — поле ввода лимита (полностью переработано для МБ)
  * `sanitize_max_upload_size()` — валидация входных данных (обновлено для мегабайт)
* Фильтр `upload_size_limit` с приоритетом 999
* Загрузка текстового домена для переводов

### 🌍 Система локализации

* Text Domain: `file-size-limit`
* Domain Path: `/languages`
* Поддержка русского (ru_RU) и английского (en_US) языков
* Обновлённые переводы для всех новых текстов интерфейса

---

## 🛠️ Решённые технические задачи

### 1. 🎛️ Удобство использования (версия 1.1.0)

**Проблема:** Ввод размера в байтах был неудобен для пользователей.
**Решение:** Полный переход на мегабайты с автоматической конвертацией существующих настроек.

### 2. 🎛️ Интеграция в настройки медиафайлов

**Проблема:** Нужно было добавить настройку в стандартную страницу WordPress.
**Решение:** Использование стандартных API WordPress для регистрации настроек в секции 'media'.

### 3. 🔝 Наивысший приоритет лимита

**Проблема:** Обеспечение того, чтобы наш лимит переопределял все другие.
**Решение:** Фильтр `upload_size_limit` с приоритетом 999 и функция `min()` для выбора минимального значения.

### 4. 🛡️ Безопасная валидация

**Проблема:** Защита от некорректных и вредоносных данных.
**Решение:** `floatval()` и `intval()` для безопасной обработки чисел, проверка на отрицательные и слишком большие значения.

### 5. 🌐 Полная локализация

**Проблема:** Поддержка множественных языков.
**Решение:** Обновлённый набор файлов переводов (.pot, .po, .mo) для русского и английского языков.

### 6. 📊 Информативность интерфейса

**Проблема:** Пользователь должен понимать текущие лимиты.
**Решение:** Отображение системных лимитов WordPress в мегабайтах и формате файла.

### 7. 🔄 Обратная совместимость (версия 1.1.0)

**Проблема:** Сохранение существующих пользовательских настроек при переходе на МБ.
**Решение:** Автоматическая конвертация байт в мегабайты при загрузке настроек.

---

## 🔧 Настройки плагина

### Где находится

**Админка → Настройки → Медиафайлы → Ограничение размера файлов**

### Доступные параметры

* **Максимальный размер файла** (в мегабайтах)
  * 0 = без ограничения
  * 0.1 - 1024 МБ = максимальный размер в мегабайтах
  * Автоматическая валидация и защита от некорректных значений
  * Шаг ввода: 0.1 МБ для точной настройки

### Примеры использования

* 2 МБ = подходит для большинства изображений
* 5.5 МБ = хороший баланс для медиафайлов
* 10 МБ = для более крупных файлов
* 0 = отключение ограничений

---

## 📡 Технические детали

### WordPress хуки

* `plugins_loaded` — загрузка текстового домена
* `admin_init` — регистрация настроек
* `upload_size_limit` — установка лимита загрузки (приоритет 999)

### Функции WordPress

* `register_setting()` — регистрация настройки
* `add_settings_section()` — добавление секции
* `add_settings_field()` — добавление поля
* `get_option()` — получение сохранённого значения
* `wp_max_upload_size()` — получение системного лимита
* `size_format()` — форматирование размера файла

### Новые функции в версии 1.1.0

* `round()` — округление значений мегабайт
* Конвертация мегабайт в байты для внутреннего хранения
* Обратная конвертация байт в мегабайт для отображения

---

## 🔐 Безопасность

* Проверка прямого доступа через `ABSPATH`
* Валидация входных данных через `floatval()` и `intval()`
* Санитизация вывода через `esc_attr()`
* Защита от XSS в пользовательском интерфейсе
* Ограничение максимального значения (1024 МБ) для предотвращения атак
* Стандартные функции WordPress для безопасности

---

## ⚡ Производительность

* Минимальное использование ресурсов
* Отсутствие внешних зависимостей
* Работа только в админ-панели для настроек
* Эффективное кеширование настроек через `get_option()`
* Быстрая конвертация единиц измерения

---

## 🧪 Разработка и отладка

* Чистый и понятный код с комментариями на русском
* Стандартные функции WordPress
* Простая архитектура без излишних абстракций
* Лёгкое тестирование и отладка
* Подробная документация для разработчиков

---

## 🚀 Что нового в версии 1.1.0

### ✨ Основные улучшения

* **Переход на мегабайты:** Полная замена байт на мегабайты для удобства пользователей
* **Улучшенный интерфейс:** Понятные единицы измерения и подсказки
* **Точная настройка:** Возможность указывать дробные значения (шаг 0.1 МБ)
* **Обратная совместимость:** Автоматическая конвертация существующих настроек
* **Расширенная валидация:** Защита от слишком больших значений

### 🔧 Технические изменения

* Обновлена логика отображения и сохранения настроек
* Добавлена конвертация МБ ↔ байты
* Улучшена валидация с поддержкой дробных чисел
* Обновлены все тексты интерфейса и переводы
* Добавлены новые сообщения об ошибках

---

## 🚀 Итог

Плагин **Ограничитель размера файлов** версии 1.1.0 представляет собой элегантное и мощное решение для контроля размера загружаемых файлов в WordPress. Главное улучшение - переход на более удобные мегабайты вместо байт - делает плагин значительно более user-friendly.

Плагин теперь:

* 🎯 **Удобен в использовании** - мегабайты вместо непонятных байт
* 🔒 **Безопасен и надёжен** - проверенная архитектура и валидация
* 🌐 **Полностью локализован** - русский и английский языки
* ⚡ **Производителен** - минимальное влияние на систему
* 🔄 **Совместим** - автоматическая конвертация старых настроек
* 📖 **Документирован** - подробная документация и комментарии

Плагин готов к продакшен-использованию и может служить надёжной основой для управления загрузкой файлов в WordPress с удобным интерфейсом на основе мегабайт.