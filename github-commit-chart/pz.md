# 📊 Пояснительная записка к плагину **GitHub Commit Chart**

**Тип документа:** Техническая пояснительная записка
**Проект:** WordPress-плагин для визуализации активности GitHub
**Версия:** 1.8.0
**Разработчик:** Владимир Бычко
**Платформа:** WordPress 6.x / PHP 8.x
**Дата:** ноябрь 2025

**🎬 Видеопрезентация:** [https://www.youtube.com/live/IKeZ9HF5hPA](https://www.youtube.com/live/IKeZ9HF5hPA)

---

## 🎯 Назначение и цели проекта

Плагин **GitHub Commit Chart** предназначен для отображения статистики разработки, полученной из GitHub-репозиториев, на страницах WordPress-сайта. Он даёт возможность встраивать интерактивную диаграмму коммитов с использованием **Gutenberg-блока** или **шорткода**, опираясь на данные публичного GitHub API.

Ключевые задачи:

* Повышение прозрачности разработки за счёт наглядной визуализации активности.
* Добавление современного, вовлекающего элемента интерфейса.
* Упрощение интеграции GitHub-данных без необходимости программирования.

---

## 🧩 Архитектура решения

Плагин построен по модульному принципу. Основные компоненты:

| Модуль                   | Файл                              | Описание                                               |
| ------------------------ | --------------------------------- | ------------------------------------------------------ |
| Основной загрузчик       | `github-commit-chart.php`         | Инициализация, регистрация хуков и подключение модулей |
| Работа с GitHub API      | `includes/github-api.php`         | Запрос коммитов, обработка данных, кэширование         |
| Регистрация блока        | `includes/block-registration.php` | Gutenberg-блок, подключение JS/CSS                     |
| Настройки администратора | `includes/admin-settings.php`     | Страница настроек, ввод токена GitHub                  |
| Фронтенд                 | `build/index.js`, `index.css`     | Визуализация данных диаграммы на стороне клиента       |

---

## ⚙️ Логика работы

1. **Инициализация**
   Основной модуль регистрирует хуки `init`, `admin_menu`, `enqueue_block_editor_assets` и подключает вспомогательные файлы.

2. **Настройки**
   В админ-панели создаётся раздел **GitHub Commit Chart**, где задаются:

   * GitHub-пользователь,
   * при необходимости — персональный токен.
     Параметры сохраняются в `wp_options`.

3. **Получение данных**
   Файл `github-api.php` выполняет запрос к GitHub REST API v3
   (`/repos/:owner/:repo/commits`) и получает данные за год.

   Технические детали:

   * используется `wp_remote_get()`;
   * при ошибках выполняется корректная обработка (graceful fallback);
   * используется кэширование через `transient` (TTL = 1 час);
   * применяется фильтрация по коммиттеру, обеспечивая отображение только личных коммитов.

4. **Отрисовка**
   Gutenberg-блок и шорткод передают JSON-данные фронтенду.
   Визуализацией занимается JS-компонент (`frontend.js`), формирующий столбчатую диаграмму:

   * ось X — недели,
   * ось Y — количество коммитов.

5. **Шорткоды**
   Поддерживаются с версии 1.4.0:

   ```
   [github-c github_profile="username"]
   ```

   Если атрибут не указан, используется значение из настроек плагина.

---

## 🧠 Технические особенности и решения

* **GitHub API:** использование фильтрации коммитов через параметр `committer`.
* **Кэширование:** уменьшение нагрузки на API, особенно без токена.
* **Безопасность:**

  * валидация всех входных данных (`sanitize_text_field()`),
  * nonce-поля в формах,
  * скрытое хранение GitHub-токена.
* **Совместимость:**
  протестировано на PHP 8.0+, WordPress 6.x, поддерживаются блоки и шорткоды.
* **Настраиваемость:**
  выбор тега заголовка блока (H2–H4 или текст без тега).
* **Локализация:**
  текущее состояние — русская версия; при необходимости возможна интеграция с WP-L10n.

---

## 🧾 Файловая структура

```
github-commit-chart/
│
├── github-commit-chart.php
├── includes/
│   ├── admin-settings.php
│   ├── block-registration.php
│   └── github-api.php
├── build/
│   ├── index.js
│   ├── frontend.js
│   ├── index.css
│   └── style-index.css
└── pz.md
```

---

## 🧩 Возможности расширения

* Добавление линейных графиков по дням или месяцам.
* Создание виджета для WordPress Dashboard.
* Поддержка приватных репозиториев через OAuth-авторизацию.
* Гибкая настройка цветовой схемы диаграммы через UI.
* Переход на **Chart.js 4** для улучшенной анимации и интерактивности.
* Добавление расширенных параметров для шорткодов (цвет, размер, тип диаграммы).

---

## 💡 Преимущества

* Простота установки и конфигурации.
* Минимальные зависимости серверной части.
* Эффективное кэширование API-результатов.
* Поддержка Gutenberg и классического редактора.
* Настраиваемые заголовки и гибкие параметры отображения.
* Высокая совместимость с актуальными версиями WordPress и PHP.

---

## ⚠️ Ограничения

* Без токена: лимит GitHub API — 60 запросов в час.
* Работает только с публичными репозиториями.
* Отображение блоков требует поддержки Gutenberg (или использования шорткодов).

---

## 🚀 Изменения по версиям

### Версия 1.8.1

* Улучшена плавность интерфейса при переключении лет:
  * Заголовок и селекторы лет остаются статичными при переключении.
  * При загрузке данных отображается плейсхолдер с индикатором загрузки.
  * Устранено "прыганье" блока диаграммы при переключении лет.
  * Добавлены плавные переходы при отображении диаграммы.

### Версия 1.8.0

* Добавлен **селектор года** для переключения между периодами коммитов.
  * Отображаются 6 предыдущих лет + текущий год (справа).
  * Селектор центрирован относительно диаграммы.
  * По умолчанию активен текущий год.
  * Коммиты предыдущих лет загружаются по запросу.
  * Плавная анимация загрузки без "прыжков" интерфейса.

### Версия 1.7.0

* Добавлена поддержка **GitHub токена**.
  Повышает лимиты до 5000 запросов/час.
  Реализована проверка токена через AJAX.

### Версия 1.6.0

* Добавлены **кликабельные ссылки** на GitHub-профили в заголовках диаграмм.
  Ссылки открываются в новой вкладке.

### Версия 1.5.2

* Улучшена фильтрация коммитов: отображаются только собственные коммиты пользователя.
* Оптимизированы API-запросы: используется параметр `committer`.
* Кэширование стабилизировано на уровне 1 часа.

### Версия 1.5.0

* Добавлен выбор тега заголовка в редакторе Gutenberg:
  H2, H3 (по умолчанию), H4 или текст без тега.
* Все варианты центрированы для эстетичной подачи.

---

## 🏁 Заключение

Плагин **GitHub Commit Chart** представляет собой лёгкое, автономное и расширяемое решение для визуализации активности GitHub-репозиториев в WordPress. Он сочетает простоту внедрения, современную графическую подачу и корректную архитектурную реализацию, соответствующую стандартам WordPress Plugin API.

Развитие функциональности — включение токена, поддержка шорткодов, расширенные настройки отображения — делает плагин удобным и универсальным инструментом для сайтов, ориентированных на открытость разработки и демонстрацию динамики проекта.