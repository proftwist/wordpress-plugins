# 📊 Пояснительная записка к плагину **GitHub Commit Chart**

**Тип документа:** Техническая пояснительная записка
**Проект:** WordPress-плагин для отображения графика коммитов GitHub
**Версия:** 1.0
**Разработчик:** Владимир Бычко
**Платформа:** WordPress 6.x / PHP 8.x
**Дата:** ноябрь 2025

---

## 🎯 Назначение и цель проекта

Плагин **GitHub Commit Chart** предназначен для визуализации активности разработки, получаемой из репозитория GitHub, непосредственно на страницах WordPress-сайта.
Он позволяет вставлять интерактивный график коммитов при помощи **блока Gutenberg**, используя данные из публичного API GitHub.

Основные цели:

* Повысить прозрачность и доверие к проекту через публичную демонстрацию активности разработки.
* Предоставить современный визуальный элемент, повышающий вовлечённость пользователей.
* Дать авторам WordPress-сайтов простой инструмент интеграции GitHub-данных без программирования.

---

## 🧩 Общая архитектура

Плагин имеет **модульную структуру**, разделённую на логические компоненты:

| Модуль                   | Файл                                | Назначение                                                         |
| ------------------------ | ----------------------------------- | ------------------------------------------------------------------ |
| Основной загрузчик       | `github-commit-chart.php`           | Инициализация плагина, подключение зависимостей, регистрация хуков |
| Работа с GitHub API      | `includes/github-api.php`           | Запрос данных о коммитах, обработка ответов API, кэширование       |
| Регистрация блока        | `includes/block-registration.php`   | Определение Gutenberg-блока, привязка JS/CSS-ресурсов              |
| Настройки администратора | `includes/admin-settings.php`       | Создание страницы настроек в админке, сохранение токена GitHub     |
| Фронтенд                 | `build/index.js`, `build/index.css` | Отрисовка графика активности коммитов на клиенте                   |

---

## ⚙️ Принцип работы

1. **Инициализация:**
   При активации плагина основной файл `github-commit-chart.php` подключает вспомогательные модули и регистрирует хуки `init`, `admin_menu` и `enqueue_block_editor_assets`.

2. **Настройки:**
   В админ-панели создаётся страница “GitHub Commit Chart”, где администратор может указать:

   * Имя пользователя GitHub
   * Имя репозитория
   * При необходимости — токен доступа

   Данные сохраняются в `wp_options`.

3. **Загрузка данных:**
   Модуль `github-api.php` обращается к GitHub REST API v3 (`/repos/:owner/:repo/stats/commit_activity`) и получает статистику коммитов за последние 52 недели.

   * Используется встроенный `wp_remote_get()`.
   * При ошибках выполняется graceful-fallback.
   * Результаты кэшируются с помощью `transient` (TTL = 12 ч).

4. **Отображение:**
   Gutenberg-блок, зарегистрированный в `block-registration.php`, загружает данные в виде JSON-массива и визуализирует их при помощи JS-графика (chart-рендерер из `frontend.js`).
   Блок доступен из панели редактора WordPress → «Виджеты → GitHub Commit Chart».

5. **Фронтенд:**
   JS-компонент строит столбчатую диаграмму, где ось X — недели, ось Y — количество коммитов.
   Стилизация управляется CSS-файлами из `build/`.

---

## 🧠 Ключевые технические решения

* **API GitHub:** используется эндпоинт `/repos/{user}/{repo}/stats/commit_activity`.
* **Кэширование:** `get_transient()` / `set_transient()` минимизирует количество обращений к API.
* **Безопасность:**

  * Все параметры проходят через `sanitize_text_field()`.
  * Используются WordPress-nonce-поля для форм.
  * Токен GitHub не выводится в открытом виде.
* **Совместимость:** проверена работа на PHP 8.0+, WordPress 6.x, поддержка Gutenberg.
* **Локализация:** используется `__()` / `_e()` для перевода интерфейса.

---

## 🧾 Файловая структура

```
github-commit-chart/
│
├── github-commit-chart.php         # основной загрузчик
├── includes/
│   ├── admin-settings.php          # настройки админ-панели
│   ├── block-registration.php      # регистрация Gutenberg-блока
│   └── github-api.php              # работа с GitHub API
├── build/
│   ├── index.js                    # логика фронтенда
│   ├── frontend.js                 # визуализация данных
│   ├── index.css / style-index.css # стили
└── pz.md                           # пояснительная записка (черновик)
```

---

## 🧩 Точки расширения и доработки

* Добавить **график по месяцам или дням** (детализировать временную шкалу).
* Реализовать **виджет Dashboard** для внутренней статистики.
* Расширить поддержку **приватных репозиториев** с использованием GitHub OAuth.
* Добавить возможность **кастомизации цветовой схемы** диаграммы.
* Перевести JS-отрисовку на **Chart.js v4** с анимацией.

---

## 💡 Преимущества

✅ Простота установки и интеграции

✅ Работа без внешних библиотек серверной части

✅ Кэширование для снижения нагрузки

✅ Поддержка Gutenberg и шорткодов

✅ Совместимость с современными версиями WP и PHP

---

## ⚠️ Ограничения

* GitHub API имеет лимит 60 запросов/час без токена.
* Отображаются только публичные репозитории.
* Требуется поддержка блок-редактора (Gutenberg).

---

## 🚀 Заключение

Плагин **GitHub Commit Chart** — лёгкое и современное решение для отображения активности GitHub-репозиториев в WordPress.
Он сочетает простоту установки, гибкость настройки и современный интерфейс визуализации.
Архитектура плагина построена по принципу «минимальной зависимости» и полностью соответствует стандартам WordPress Plugin API.